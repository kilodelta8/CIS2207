#include "StackInterface.h"
/*
//stack implementation declarations
template<class ItemType>
bool pushStack(const ItemType& newEntry)
{
	bool success = true;
	for (int i = 0; i < userInput.lenght(); i++)
	{
		if (objStack[i] == "" || objStack[i] == nullptr)
		{
			objStack[i] = newEntry;
			success = true;
			break;
		}
		else
		{
			success = false;
		}
	}
	return success;
}

template<class ItemType>
bool StackInterface<ItemType>::pop()
{
	bool success = true;
	int size = userInput.length();
	objStack[size] = "";
	if (obj.Stack[size] == "")
		success = true;
	else
		success = false;
	return success;
}

template<class ItemType>
ItemType StackInterface<ItemType>::peek()
{
	return objStack;
}
//TODO - Dont forget the destruct for the stack - SEE PalindromRecognition.h*/


